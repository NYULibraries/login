machine:
  services:
    - docker

dependencies:
  override:
    - docker-compose version

database:
  override:
    - echo 'Do nothing'

test:
  override:
    - docker-compose run -e RAILS_ENV=test web rake
