language: ruby

rvm:
  - 1.9.3
  - 2.1.0

env:
  - LOGIN_DB_USER=postgres LOGIN_SECRET_TOKEN=33032bbf4b90e9c1e7bddbfdec0ea9ae1e3626989ff9c86ced32c23fc5ceded08c3969c011604401b5aeaf742e6713627db6adb0765798e4c6d6e8b895ec96c6

before_script:
  - psql -c 'create database login_test;' -U $LOGIN_DB_USER
  - bundle exec rake db:schema:load RAILS_ENV=test

bundler_args: --without development

script: "bundle exec rake"

notifications:
  irc: 
    channels:
      - "irc.freenode.org#libtechnyu"
    template:
      - "%{repository}#%{build_number} (%{branch} - %{commit} : %{author}): %{message}"
      - "Change view : %{compare_url}"
      - "Build details : %{build_url}"
  email:
    - web.services@library.nyu.edu
